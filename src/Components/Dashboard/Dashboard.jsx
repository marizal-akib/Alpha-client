import { Helmet } from "react-helmet-async";

const Dashboard = () => {
  return (
    <div>
      <Helmet>
        <title>Alpha | Dashboard</title>
      </Helmet>
    </div>
  );
};

export default Dashboard;
